import * as React from 'react';
import { render } from 'react-dom';
import { IndexComponent } from './Core/IndexComponent';

render(
    <IndexComponent />,
    document.getElementById('clean.application')
);
